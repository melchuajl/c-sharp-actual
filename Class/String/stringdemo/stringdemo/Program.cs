using System.Text; // This import is required before you can use StringBuilder

string str = "Here is a string yo";

Console.WriteLine(str.Replace("string", "integer")); 

StringBuilder sentence = new("Here's the default value");

Console.WriteLine(sentence);